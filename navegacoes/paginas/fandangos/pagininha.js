import { View, StyleSheet } from "react-native";

export function pagininha(){
    return(
        <View style={styles.container}>
            <Text>
                Calvicie de anão
            </Text>
        </View>
     
     
    )}
    const styles = StyleSheet.create({
        container: {
          flex: 1,
          backgroundColor: '#fff',
          alignItems: 'center',
          justifyContent: 'center',
        },
      });
